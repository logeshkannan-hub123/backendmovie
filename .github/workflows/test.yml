on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - run: npm install

      - run: npm test
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
